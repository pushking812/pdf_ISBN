# Настройка терминала Windows 10 для поддержки UTF-8 и эмодзи

## Проблема
Ошибка `UnicodeEncodeError: 'charmap' codec can't encode character '\U0001f50d'` возникает из-за того, что терминал использует кодовую страницу 866 (DOS Cyrillic), которая не поддерживает эмодзи.

## Решение: настроить терминал на использование UTF-8

### Вариант 1: Быстрое исправление (вручную перед каждым запуском)
```powershell
chcp 65001
```

### Вариант 2: Настройка профиля PowerShell (рекомендуется)

#### Шаг 1: Создать профиль PowerShell (если его нет)
```powershell
# Проверить существование профиля
Test-Path $PROFILE

# Создать профиль, если его нет
if (!(Test-Path -Path $PROFILE)) {
    New-Item -ItemType File -Path $PROFILE -Force
}
```

#### Шаг 2: Добавить настройки UTF-8 в профиль
```powershell
# Открыть профиль в блокноте
notepad $PROFILE
```

Добавить следующие строки:
```powershell
# Установить кодировку UTF-8 для PowerShell
$OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::InputEncoding = [System.Text.Encoding]::UTF8

# Установить кодовую страницу 65001 (UTF-8)
chcp 65001 | Out-Null

# Установить шрифт, поддерживающий эмодзи (если используется Windows Terminal)
if ($Host.Name -eq 'ConsoleHost') {
    # Для обычного PowerShell
    # Рекомендуется использовать Windows Terminal для лучшей поддержки эмодзи
}
```

#### Шаг 3: Сохранить и перезагрузить PowerShell
```powershell
# Перезагрузить профиль
. $PROFILE
```

### Вариант 3: Настройка Windows Terminal (самый современный подход)

#### Шаг 1: Установить Windows Terminal
- Из Microsoft Store: поиск "Windows Terminal"
- Или через Winget: `winget install Microsoft.WindowsTerminal`

#### Шаг 2: Настроить Windows Terminal
1. Откройте Windows Terminal
2. Нажмите `Ctrl+,` для открытия настроек
3. В файле `settings.json` найдите профиль PowerShell
4. Добавьте/измените следующие параметры:
```json
{
    "profiles": {
        "defaults": {
            "font": {
                "face": "Cascadia Code",
                "size": 12
            },
            "experimental.retroTerminalEffect": false
        },
        "list": [
            {
                "guid": "{61c54bbd-c2c6-5271-96e7-009a87ff44bf}",
                "name": "Windows PowerShell",
                "commandline": "powershell.exe",
                "hidden": false,
                "fontFace": "Cascadia Code",
                "fontSize": 12,
                "colorScheme": "Campbell"
            }
        ]
    }
}
```

#### Шаг 3: Установить шрифт с поддержкой эмодзи
1. Скачать и установить шрифт "Cascadia Code" из GitHub:
   https://github.com/microsoft/cascadia-code/releases
2. Или использовать "Consolas" с поддержкой эмодзи (в Windows 10 1903+)

### Вариант 4: Системные настройки Windows 10

#### Включить бета-функцию "Использовать Юникод UTF-8 для поддержки языка worldwide"
1. Откройте **Панель управления** → **Часы, язык и регион** → **Регион**
2. Перейдите на вкладку **Административные**
3. Нажмите **Изменить язык системы...**
4. Установите флажок **Бета-версия: Использовать Юникод UTF-8 для поддержки языка worldwide**
5. Нажмите **OK** и перезагрузите компьютер

**Внимание**: Эта настройка может повлиять на некоторые старые приложения.

### Вариант 5: Настройка через реестр (для опытных пользователей)

Создайте файл `utf8.reg`:
```registry
Windows Registry Editor Version 5.00

[HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Nls\CodePage]
"ACP"="65001"
"OEMCP"="65001"
"MACCP"="65001"
```

Запустите файл от имени администратора и перезагрузите компьютер.

## Проверка настроек

После применения любого из решений проверьте:

```powershell
# Проверить кодовую страницу
chcp

# Проверить кодировки в PowerShell
[System.Text.Encoding]::Default.EncodingName
[System.Console]::OutputEncoding.EncodingName

# Тест вывода эмодзи
python -c "print('🔍 ✅ ❌ ⚠️ Тест эмодзи')"
```

## Рекомендации

1. **Для быстрого решения**: используйте `chcp 65001` перед запуском Python-скриптов
2. **Для постоянного решения**: настройте профиль PowerShell (Вариант 2)
3. **Для лучшего опыта**: установите Windows Terminal и шрифт Cascadia Code (Вариант 3)
4. **Для системных изменений**: включите UTF-8 поддержку в настройках региона (Вариант 4)

## Примечание для текущей задачи

После настройки терминала запустите:
```powershell
chcp 65001
py .\main.py _books
```

Ошибка UnicodeEncodeError должна исчезнуть, так как терминал будет поддерживать эмодзи.