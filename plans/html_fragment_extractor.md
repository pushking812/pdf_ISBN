# План разработки модуля извлечения HTML-фрагмента по ключевому тексту

## Цель
Создать отдельный модуль с функцией, которая по ссылке на страницу ресурса, ключевому тексту и глубине вложения возвращает фрагмент HTML с тегами на указанную глубину, содержащий ключевой текст.

## Контекст проекта
Проект `pdf_ISBN` занимается извлечением ISBN из PDF‑файлов и скрапингом данных о книгах с интернет‑ресурсов (Читай‑город, Book.ru, РГБ). Основной код скрапинга находится в `scraper.py`, использует Selenium WebDriver и BeautifulSoup для парсинга.

## Уточнённые требования
1. **Входные параметры:**
   - `url` (строка) – ссылка на страницу ресурса.
   - `key_text` (строка) – слово или набор слов, отображаемый на странице.
   - `depth` (целое число) – глубина вложения ключевого текста в теги, измеряемая от самого текстового узла:
     - `depth = 0` – текстовый узел,
     - `depth = 1` – родительский тег,
     - `depth = 2` – тег‑родитель родительского тега и т.д.

2. **Выходные данные:**
   - Фрагмент HTML в виде строки, содержащий все теги на указанной глубине, а внутри них ключевой текст.

3. **Отдельный модуль:**
   - Функция пока не должна быть жёстко связана с основным функционалом, но должна быть спроектирована так, чтобы её можно было легко встроить в существующий код.

4. **Анализ мест использования:**
   - Отладка селекторов при разработке парсеров.
   - Автоматическое построение селекторов на основе ключевого текста.
   - Использование полученных селекторов в основном коде (например, в `scraper.py` или `resources.py`).

## Архитектурное решение

### Модуль `html_fragment.py`
Разместим в корне проекта (рядом с `utils.py`), чтобы его можно было импортировать отовсюду.

#### Основные функции

1. **`find_text_nodes(soup: BeautifulSoup, text: str, exact: bool = True) -> List[PageElement]`**  
   Находит все текстовые узлы (NavigableString), содержащие заданный текст. Если `exact=True` – точное совпадение, иначе – частичное.

2. **`get_ancestor(element: PageElement, depth: int) -> Optional[Tag]`**  
   Возвращает тег‑предка на заданной глубине. Если `depth == 0` и элемент является тегом – возвращает сам элемент, если текстовый узел – возвращает родительский тег (глубина 1). Если глубина превышает число доступных предков – возвращает самый дальний предок (например, `<html>`).

3. **`extract_html_fragment(html: Union[str, BeautifulSoup], key_text: str, depth: int = 1, exact: bool = True, all_matches: bool = False) -> Union[Optional[str], List[str]]`**  
   Основная функция. Принимает HTML (строку или объект BeautifulSoup), находит ключевой текст, поднимается на заданную глубину и возвращает внешний HTML найденного тега. Если `all_matches=True` – возвращает список фрагментов для всех найденных вхождений, иначе – первый найденный фрагмент (или `None`).

4. **`extract_html_fragment_from_url(url: str, key_text: str, depth: int = 1, use_selenium: bool = False, driver=None, **request_kwargs) -> Optional[str]`**  
   Удобная обёртка, которая загружает страницу по URL (через `requests` или Selenium) и вызывает `extract_html_fragment`. Параметр `use_selenium` определяет, использовать ли динамический рендеринг. Если передан `driver`, используется он.

5. **`extract_html_fragment_from_driver(driver, key_text: str, depth: int = 1) -> Optional[str]`**  
   Извлекает фрагмент из уже загруженной страницы Selenium WebDriver (берёт `driver.page_source`).

#### Интерфейсы для интеграции

- **Использование в `scraper.py`:**  
  В методе `_parse_book_page` можно добавить fallback‑логику: если стандартные селекторы не сработали, искать по ключевым словам («ISBN», «Страниц», «Год») и извлекать нужные данные из полученного фрагмента.

- **Использование в `resources.py`:**  
  В кастомных парсерах (например, для Book.ru) можно применять функцию для поиска элементов по текстовым меткам.

- **Использование в тестах и отладке:**  
  Создать скрипт `debug_selectors.py`, который для заданного URL и ключевого текста выводит фрагменты на разных глубинах – это поможет быстро подбирать селекторы.

### Схема работы

```mermaid
flowchart TD
    A[Вход: URL, key_text, depth] --> B{use_selenium?}
    B -->|Да| C[Загрузить через Selenium]
    B -->|Нет| D[Загрузить через requests]
    C --> E[Получить page_source]
    D --> E
    E --> F[Парсинг BeautifulSoup]
    F --> G[Найти текстовые узлы<br/>содержащие key_text]
    G --> H{Узлы найдены?}
    H -->|Нет| I[Вернуть None]
    H -->|Да| J[Для каждого узла<br/>подняться на depth уровней]
    J --> K[Взять внешний HTML тега]
    K --> L[Вернуть фрагмент<br/>(или список фрагментов)]
```

## План реализации

1. **Создание модуля `html_fragment.py`**  
   - Написать функцию `find_text_nodes`.  
   - Написать функцию `get_ancestor`.  
   - Реализовать основную функцию `extract_html_fragment`.  
   - Добавить обёртки для URL и драйвера.  

2. **Тестирование**  
   - Написать юнит‑тесты для всех функций (поместить в `tests/test_html_fragment.py`).  
   - Проверить работу на реальных страницах (Читай‑город, Book.ru).  
   - Убедиться, что глубина вложения считается корректно.  

3. **Интеграция в проект**  
   - Проанализировать, где в `scraper.py` и `resources.py` можно использовать новую функцию.  
   - Добавить пример использования в комментариях модуля.  
   - Создать скрипт‑пример `examples/debug_selectors.py`.  

4. **Документация**  
   - Добавить docstrings на русском языке.  
   - Описать типы параметров и возвращаемых значений (аннотации типов).  
   - Указать, как импортировать и вызывать функцию.  

## Оценка сложности
- Реализация основных функций – 1‑2 дня.
- Тестирование и отладка – 1 день.
- Интеграция и документация – 0.5 дня.

## Следующие шаги
1. Утвердить данный план.
2. Переключиться в режим **💻 Code** для реализации модуля.
3. После реализации протестировать и интегрировать функцию в основной код (при необходимости).